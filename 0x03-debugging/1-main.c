#include <stdio.h>

/**
 * main - Demonstrates an infinite loop and then avoids it.
 *
 * Return: Always 0 (indicating successful completion)
 */
int main(void)
{
    int i;

    printf("Infinite loop incoming :(\n");
    i = 0;

    /* This code creates an infinite loop */
    /* while (i < 10) */
    /* { */
    /*     putchar(i); */
    /* } */

    printf("Infinite loop avoided! \\o/\n");
    return (0);
}
